*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
/External
Homestead.json
Homestead.yaml
Thumbs.db

# Claude Code local config
.claude/

# Planning board
Planning/
# Rapports d'audit de sécurité : cartographie des faiblesses + scénarios
# d'exploitation + ce qui reste à corriger. Ne JAMAIS versionner ni déployer
# (le git pull de deploy.sh les déposerait dans le webroot de production).
audit/
docs/audit-secu-fable/
# SQLite database
database/*.sqlite

# Local artifacts (smoke test reports, screenshots, Lighthouse reports, etc.)
/tmp/

# Snapshots de prerendering (générés au déploiement, transférés par rsync — pas versionnés)
/public/prerendered

# Sauvegarde locale du script de déploiement
deploy.sh.bak

# Pièces de dossiers (marque, juridique) : gardées en local, hors dépôt
docs/*.pdf

# Script de déploiement web obsolète : ne JAMAIS versionner (exécution de code non authentifiée)
/public/deploy.php
